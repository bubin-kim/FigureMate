services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: figuremate
      POSTGRES_PASSWORD: figuremate
      POSTGRES_DB: figuremate
    ports:
      - "5432:5432"
    volumes:
      - figuremate_pgdata:/var/lib/postgresql/data

volumes:
  figuremate_pgdata:
